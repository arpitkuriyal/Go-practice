# Polymorphic Shape Calculator

`Shape` is implemented by circles, rectangles, and triangles. Constructors validate dimensions, while `TotalArea` works with any `Shape` implementation.

```bash
go test ./challenges/06-shape-calculator
```
