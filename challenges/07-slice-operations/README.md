# Slice Operations

Generic helpers for inserting, removing, filtering, and deduplicating slices. Each operation returns a new slice so callers do not get unexpected backing-array mutations.

```bash
go test ./challenges/07-slice-operations
```
