# Employee Management

## Challenge

Create an `Employee` struct and a `Manager` that can:

- add an employee;
- remove an employee by ID;
- find an employee by ID; and
- calculate the average salary.

Consider the edge case of an empty employee list before looking at the implementation in `data-management.go`.

```bash
go run ./challenges/03-employee-management
```
